import styled from '@emotion/styled'

const Frame = styled.div`
    background-color: red;
`;

const Action = styled.button`
    color: red;
`;

const Title = styled.h1`
    color: blue;
`;

export { Frame, Action, Title }